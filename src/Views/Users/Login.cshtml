@page 
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    Layout = "_Layout";
}
<div class="authReg_background">
    <h2 >Авторизация</h2>
    <div class="authReg_window">

        <form asp-action="Login" asp-controller="Users" class="auth-form" name="form-auth" method="post">

            
            <label class="input-form__label">
                <span class="input-placeholder">email</span>
                <input class="form__input input-email" type="email" name="Email" autocomplete="off" required  value="kotus777@mail.ru">
            </label>

            <label class="input-form__label">
                <span class="input-placeholder">пароль</span>
                <input class="form__input input-password" type="password" name="HashPass" autocomlete="off" required value="1">
                <div class="auth-form__toggler">
                    <i class="la la-eye auth-form__icon"></i>
                    <input type="checkbox" class="auth-form__checkbox password-toggler">
                </div>
            </label>

            @if (ViewBag.error != null){
                <div class="auth-form__answer">
                    <p class="text-danger">@ViewBag.error</p>
                </div>
            }

            <input class="auth-form__submit" type="submit" value="Войти">

            <a class="auth-form__link" href="/Users/PasswordRecoveryRequest">Забыли пароль?</a>
            <div class="auth-form__bottom">
                <span>У вас еще нет аккаунта?</span>
                <a href="/Users/Registration">Создать новый</a>
            </div>
        </form>
    </div>
</div>

<div class="hint"></div>
